class Solution:
    def search(self, nums: List[int], target: int) -> int:
        if nums[mid] >= nums[left] 
            if target < nums[left]:
                left = mid + 1
            elif target
        else:


        